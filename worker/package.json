{
  "name": "mi-portafolio-contact",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:contact": "wrangler deploy --config wrangler.contact.toml"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}