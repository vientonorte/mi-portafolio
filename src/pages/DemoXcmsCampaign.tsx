/**
 * Landing de demo X|CMS para campañas (Google Ads / SEO / LinkedIn SEM).
 * Alias canónico de /demo/prototype — mismo gate y reloj de 5 min.
 */
import TimedServiceDemo from "./TimedServiceDemo";

export default function DemoXcmsCampaign() {
  return <TimedServiceDemo forcedPath="prototype" />;
}
