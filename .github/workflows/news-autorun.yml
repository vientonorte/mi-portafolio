# DEPRECATED 2026-09-09: /s/news removed; do not regen.
# LinkedIn weekly CTA = /s/consultoria with UTMs. Covers live in vault SEM/news-covers.
name: News autorun · LinkedIn SEO (DEPRECATED)

# schedule commented — do not re-commit public/s/news
on:
  # schedule:
  #   - cron: "0 13 * * 2"
  #   - cron: "0 12 * * 6"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: news-autorun
  cancel-in-progress: false

jobs:
  publish:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated no-op
        run: |
          echo "DEPRECATED 2026-09-09: /s/news removed; do not regen" >> "$GITHUB_STEP_SUMMARY"
          exit 0
