# vn-ads-query (Fase 2)
Isolated Google Ads metrics query. FO Vite untouched.
LIVE read-only GAQL. No mutate. No spend.
Without complete .env -> MockAdsClient (Fase 1).
Factory: Live if required GOOGLE_ADS_* set, else Mock.
Key names listed in .env.example. Never paste values.
Missing auth: Ads API Center + Cloud Console.
ssot live JSON files are gitignored.
