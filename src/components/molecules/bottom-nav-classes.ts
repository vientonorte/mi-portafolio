/**
 * Dock inferior liquid glass (mobile + desktop).
 * Estilos de material en global.css (.bottom-nav-mobile / .liquid-nav-cta).
 * El FAB «subir» flota encima (BackToTop).
 */
export const BOTTOM_NAV_BASE_CLASS =
  "bottom-nav-mobile--glass fixed bottom-0 left-0 right-0 z-[60]";

export const BOTTOM_NAV_DOCK_CLASS = "";
